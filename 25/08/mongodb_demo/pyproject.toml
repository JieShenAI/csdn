[project]
name = "mongodb-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pymongo>=4.14.0",
]

[dependency-groups]
dev = [
    "ipykernel>=6.30.1",
]
